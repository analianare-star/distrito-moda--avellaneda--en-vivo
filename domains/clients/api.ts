import { api } from '../../services/api';

export const fetchClientState = api.fetchClientState;
export const addFavorite = api.addFavorite;
export const removeFavorite = api.removeFavorite;
export const addReminder = api.addReminder;
export const removeReminder = api.removeReminder;
